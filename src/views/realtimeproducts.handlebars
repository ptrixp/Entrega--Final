<style>
    /* Estilo para centrar el texto, aumentar el tamaño y ponerlo en negrita */
.text {
    text-align: center;   
    font-size: 18px;       
    font-weight: bold;     
    margin: 10px 0;       
}
    /* Estilos generales */
    body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f9;
        color: #333;
        margin: 0;
        padding: 20px;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    h2 {
        color: #333;
        margin-bottom: 15px;
    }

    /* Contenedor principal con dos columnas */
    .contenedorPrincipal {
        display: grid;
        grid-template-columns: 1fr 1fr; /* Dos columnas */
        gap: 20px;
        width: 100%;
        max-width: 1200px;
        margin-bottom: 20px;
    }

    /* Títulos en la primera fila (columna izquierda) */
    .tituloColumna {
        grid-column: span 2; /* Hace que los títulos ocupen ambas columnas */
        text-align: center;
        margin-bottom: 20px;
    }

    /* Contenedor de productos en la primera columna */
    #contenedorProductos {
        display: flex;
        flex-wrap: wrap;
        gap: 15px;
        justify-content: flex-start;
        padding: 20px;
    }

    /* Estilo para cada tarjeta de producto */
    .producto {
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 15px;
        width: 200px;
        text-align: center;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        transition: transform 0.2s;
    }

    .producto:hover {
        transform: translateY(-5px);
    }

    .producto p {
        margin: 5px 0;
    }

    /* Estilo para el botón Eliminar */
    .eliminarBtn {
        background-color: #ff4d4d;  /* Fondo rojo */
        color: #fff;                /* Texto blanco */
        border: none;               /* Sin bordes */
        padding: 10px 20px;         /* Espaciado interno */
        border-radius: 5px;         /* Bordes redondeados */
        font-weight: bold;          /* Texto en negrita */
        cursor: pointer;           /* Manito al pasar sobre el botón */
        display: block;             /* Hace que el botón ocupe todo el espacio disponible */
        margin: 0 auto;             /* Centrado horizontal */
        width: fit-content;         /* Ajusta el ancho al contenido */
        transition: background-color 0.2s;  /* Efecto de transición */
    }

    /* Efecto al pasar el ratón */
    .eliminarBtn:hover {
        background-color: #ff1a1a;  /* Cambio a un rojo más oscuro al hacer hover */
    }

    /* Formulario */
    form {
        display: flex;
        flex-direction: column;
        gap: 10px;
        background-color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        width: 250px;
        margin-bottom: 20px;
    }

    form input, form select, form button {
        padding: 10px;
        font-size: 14px;
        border-radius: 5px;
        border: 1px solid #ccc;
    }

    form input:focus, form select:focus {
        outline: none;
        border-color: #333;
    }

    form button {
        background-color: #4CAF50;
        color: #fff;
        font-weight: bold;
        cursor: pointer;
        transition: background-color 0.2s;
    }

    form button:hover {
        background-color: #45a049;
    }

    /* Asegura que el formulario se ajuste bien en pantallas pequeñas */
    @media (max-width: 768px) {
        .contenedorPrincipal {
            grid-template-columns: 1fr; /* En pantallas pequeñas, usar una sola columna */
        }
    }
</style>

<!-- HTML -->
<div class="contenedorPrincipal">
    <!-- Primera fila: Títulos (ocupa las dos columnas) -->
    <div class="tituloColumna">
        <h2>Real Time Products</h2>
        <h2>Productos actualizados en tiempo real</h2>
    </div>

    <!-- Segunda fila: Contenedor de productos en la primera columna -->
    <div id="contenedorProductos">
        <!-- Productos aparecerán aquí -->
    </div>

    <!-- Segunda columna: Formulario para agregar productos -->
    <div>
        <h2>  ¡ Agrega un producto !</h2>
        <form>
            <input type="text" id="title" placeholder="Titulo">
            <input type="text" id="description" placeholder="Descripción">
            <input type="number" id="price" placeholder="Precio">
            <input type="text" id="img" value="Sin imagen">
            <input type="text" id="code" placeholder="Código">
            <input type="number" id="stock" placeholder="Stock">
            <input type="text" id="category" placeholder="Categoria">
            <select id="status">
                <option value="true"> Activo </option>
                <option value="false"> Inactivo </option>
            </select>
            <button type="button" id="btnEnviar">Enviar</button>
        </form>
    </div>
</div>

<script src="https://cdn.socket.io/4.7.4/socket.io.min.js"></script>
<script src="js/index.js"></script>